services:
  assistant-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=local-docker
      - PORT=8080
      - LOG_LEVEL=info
      - CATALOG_PATH=app/data/products.json
